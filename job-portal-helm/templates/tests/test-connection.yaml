apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "job-portal-helm.fullname" . }}-test-connection"
  labels:
    {{- include "job-portal-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "job-portal-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
