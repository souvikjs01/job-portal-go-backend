apiVersion: v1
kind: Service
metadata:
  name: {{ include "job-portal-helm.fullname" . }}
  labels:
    {{- include "job-portal-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "job-portal-helm.selectorLabels" . | nindent 4 }}
